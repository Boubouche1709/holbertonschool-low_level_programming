#include "main.h"
#include <stdio.h>

/**
 * _isupper - Entry of the code
 *
 * @c: Character to check
 *
 * Return: 1 for success, 0 otherwise
 */

int _isupper(int c);
{
if (c >= 'A' && c <= 'Z')
return (1);
else
return (0);
}
