#include <stdio.h>
#include "main.h"
#include <unistd.h>
#include <stdlib.h>
/**
 * _abs - print the absolute value
 * Return: absolute value
 */
int _abs(int i)
{
if (i > 0)
{
return (abs(i));
}
else if (i < 0)
{
return (abs(i));
}
else
{
return (abs(i));
}
}
